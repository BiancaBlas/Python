chars = ("a", "b", "c") # the parenteces are only required when creating empty or single element tuples
#() are for tuples, [] are for lists
even_number = (0, 2, 4, 6, 8)
#tuples are immutable, you can not change, add or delete items
print(even_number[1])
